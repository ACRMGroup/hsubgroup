1. Add code to ignore X in input sequence - use -x flag to restore
corrent behaviour of matching against an X if there is one.

2. Add code to regenerate the matrices with log values (with a +1
offset) and then add the logs (-p flag)

